# yaml-language-server: $schema=https://taskfile.dev/schema.json
---
version: "3"

includes:
  chezmoi: .taskfiles/ChezmoiTasks.yaml

tasks:
  default:
    desc: List all tasks.
    cmd: task --list-all
